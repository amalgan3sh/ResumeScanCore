# requirements.txt
Flask==2.0.1
flask-cors==4.0.0
pdfminer.six==20221105
nltk==3.8.1
gunicorn==20.1.0

# render.yaml
services:
  - type: web
    name: ats-analyzer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
